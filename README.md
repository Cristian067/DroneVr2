El archivo python esta en la carpeta scripts.
Hay que cambiar la ip del archivo de python a la ip del equipo, iniciar el archivo python y hacer una build cambiando la ip en el script controller dentro del jugador.
Controlar el dron funciona, pero no se ve la camara del dron desde el plano, no se sabe el porque, en unity si funciona
